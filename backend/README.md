# Pathfinder Backend

Node.js + Express backend using MongoDB (Mongoose) for Pathfinder AI.

## Getting Started

1. \`npm install\`
2. Set up your \`.env\` file (use \`.env.example\` as a reference)
3. \`npm run dev\` (Requires nodemon, or just \`node server.js\`)
